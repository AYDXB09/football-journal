# Design mockups

`football_reflection_system.html` — the standalone HTML design mockup that preceded the real Next.js rebuild (`src/`). Open it directly in a browser; no build step or dependencies needed. Kept as provenance for how the app's visual language and interaction model were worked out before implementation.
